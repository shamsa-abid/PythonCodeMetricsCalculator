def odd_count(lst):
    res = ["the number of odd elements {}n the str{}ng {} of the {}nput.".format(sum(int(d) % 2 == 1 for d in arr), sum(
        int(d) % 2 == 1 for d in arr), sum(int(d) % 2 == 1 for d in arr), sum(int(d) % 2 == 1 for d in arr)) for arr in lst]
    return res
